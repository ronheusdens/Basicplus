PROCEDURE CalcFunc(X)
    LET result = X^5 + 2*X^3 - 1
    RETURN result
END PROCEDURE

READ N
LET A = 0: B = 1: X = 0.5
FOR I = 1 TO N
    CalcFunc(X)
    IF result > 0 THEN
        LET B = X
    ELSE
        LET A = X
    ENDIF
    LET X = (A+B)/2
NEXT I
CalcFunc(X)
PRINT X, result
DATA 20
END
